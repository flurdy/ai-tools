# Pi model-tier router moved

The router now lives in the standalone [`flurdy/pi-model-tier-router`](https://github.com/flurdy/pi-model-tier-router) repository so this repository can remain a collection of independent AI tools.

The standalone package remains private and Git-only while it is dogfooded. Router source history before extraction remains available in this repository through commit `ea88182`.
