# Pi skill model router moved

The router now lives in the standalone [`flurdy/pi-skill-model-router`](https://github.com/flurdy/pi-skill-model-router) repository so this repository can remain a collection of independent AI tools.

The standalone package is public and Git-only while it is dogfooded. Router source history before extraction remains available in this repository through commit `ea88182`.
